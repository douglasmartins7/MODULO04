//Arquivo de entrada, tudo inicia daqui a aplicação
//const soma = (a, b) => a + b;
//alert(soma(1, 7));

//old
//var soma = function soma(a, b) {
//return a + b;
//};

import React from 'react';
import { render } from 'react-dom';

render(<h1>Hello World</h1>, document.getElementById('app')); // ele renderiza um componente react dentro de do html
